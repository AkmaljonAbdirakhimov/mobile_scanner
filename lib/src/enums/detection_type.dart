/// This enum defines the different detection types for the mobile scanner.
enum DetectionType {
  /// Barcode and QR code detection
  barcode,

  /// Credit card detection (OCR-based)
  creditCard,
}
